let User;_898‍.x([["createUser",()=>createUser],["authUser",()=>authUser]]);_898‍.w("../ressources/user/user.model",[["User",["User"],function(v){User=v}]]);// IMPORT MODEL


const createUser = async (req,res) =>{
  let user;
  try {
    user = await User.findOne({ 'email': req.body.email });
  } catch(e){
    _898‍.g.console.error(e)
    res.end()
  }
  if (user) {
    console.error('This email is already taken')
    res.end()
  } else {
    // USER CREATION
    let newUser = new User();

    newUser.email = req.body.email;
    newUser.username = req.body.username;
    newUser.password = newUser.generateHash(req.body.password);
    try{
      await User.create(newUser);
      res.status(201).json(newUser);
    }catch(e){
      _898‍.g.console.error(e);
      res.status(400).end();
    }
  }

} 
const authUser = async (req,res) => {
  let user;
  try {
    user = await User.findOne({ 'email': req.body.email });
  } catch(e){
    _898‍.g.console.error(e)
    return false;
  }
  if (user) {
    if (!user.validPassword(req.body.password)) {
      console.error('Wrong password');
      return false;
    } else if (user.validPassword(req.body.password)) {
      return true;
    } else {
      console.error('An error occured');
      return false;
    }
  } else {
    console.error('This user does not exist');
    return false;
  }
}

